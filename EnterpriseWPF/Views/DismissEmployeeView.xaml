<mah:MetroWindow x:Class="EmployeesWPF.Views.DismissEmployeeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"                 
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:EmployeesWPF.Views"
        mc:Ignorable="d"
        Name="DismissalWindow"
        Title="Zwolnienie pracownika" Height="120" Width="340" MinHeight="120" MinWidth="340" MaxHeight="120" MaxWidth="340">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Margin="5" Content="Data zwolnienia"/>
        <DatePicker Margin="5" Grid.Column="1"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
            <Button Margin="5" Content="Zatwierdź" Width="100"/>
            <Button Margin="5" Content="Anuluj" Width="100"/>
        </StackPanel>
        
    </Grid>
</mah:MetroWindow>
